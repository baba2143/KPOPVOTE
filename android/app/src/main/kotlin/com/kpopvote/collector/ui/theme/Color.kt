package com.kpopvote.collector.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Brand palette extracted from iOS Constants.swift (ios/KPOPVOTE/Utilities/Constants.swift).
 * Keep in sync with res/values/colors.xml.
 */
object BrandColors {
    val PrimaryBlue = Color(0xFF1976D2)
    val PrimaryPink = Color(0xFFE91E63)
    val BackgroundDark = Color(0xFF0A1628)
    val CardDark = Color(0xFF1A2744)
    val AccentPink = Color(0xFFFF1F8F)
    val AccentBlue = Color(0xFF00D4FF)
    val BackgroundLight = Color(0xFFF5F5F5)
    val TextGray = Color(0xFF9CA3AF)
    val GradientPurple = Color(0xFF7C3AED)
    val StatusUrgent = Color(0xFFEF4444)
    val StatusPending = Color(0xFF2196F3)
    val StatusCompleted = Color(0xFF4CAF50)

    // Derived
    val SurfaceVariantDark = Color(0xFF243555)
    val SurfaceVariantLight = Color(0xFFE0E0E0)
    val OutlineDark = Color(0xFF374151)
    val OutlineLight = Color(0xFFBDBDBD)
    val OnBackgroundLight = Color(0xFF1A1A1A)
}
