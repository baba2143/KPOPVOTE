# 📱 Votesタブ（Community/Discover）機能設計仕様書

**作成日**: 2025年1月13日
**バージョン**: 1.0
**ステータス**: Phase 1実装待ち
**関連**: Phase 1 iOS開発

---

## 📋 目次

1. [概要](#1-概要)
2. [コンセプトと目的](#2-コンセプトと目的)
3. [機能詳細](#3-機能詳細)
4. [データモデル](#4-データモデル)
5. [API仕様](#5-api仕様)
6. [UI設計](#6-ui設計)
7. [Firestoreスキーマ](#7-firestoreスキーマ)
8. [既存機能との連携](#8-既存機能との連携)
9. [実装ロードマップ](#9-実装ロードマップ)
10. [技術スタック](#10-技術スタック)

---

## 1. 概要

### 1.1 機能名
**Votesタブ**（内部名: Community/Discover機能）

### 1.2 配置場所
- 下部タブバー：左から2番目
- タブアイコン：`chart.bar` or `person.3.fill`（変更可能）
- タブラベル：「Votes」 or 「Discover」 or 「Community」

### 1.3 一言説明
> **"投票タスクのソーシャルプラットフォーム"**
> ユーザーが投票タスクをキュレーションし、共有することで、コミュニティ全体で投票活動を盛り上げる機能

### 1.4 現状と課題
**現状**: 「Coming Soon」プレースホルダー画面のみ実装
**課題**: TASKSタブと機能が重複すると思われていたが、実際には異なる役割

### 1.5 TASKSタブとの違い

| 項目 | Votesタブ（Community/Discover） | TASKSタブ |
|------|--------------------------------|-----------|
| **主な役割** | コミュニティからの発見・共有 | 個人のタスク管理 |
| **データソース** | 他ユーザーの共有コレクション | 自分が登録したタスク |
| **主なアクション** | 検索・保存・一括追加 | 閲覧・完了・更新 |
| **ユーザー体験** | ソーシャル・発見 | パーソナル・管理 |

---

## 2. コンセプトと目的

### 2.1 核となる価値提案

#### 🔍 **発見**: 他のファンがまとめた投票を簡単に発見
- 「BTS MAMA 2024 完全投票パック」のようなキュレーションされたコレクション
- アーティスト別、イベント別、緊急度別の分類
- トレンド・人気順での表示

#### 🤝 **共有**: 自分のタスクリストをコミュニティに共有
- マイコレクションとして公開
- タグ付けで発見されやすく
- いいね・保存数でエンゲージメント測定

#### ⚡ **効率化**: ワンタップで全タスクを一括追加
- コレクション内の全投票タスクを自分のTASKSに追加
- 個別に登録する手間を削減
- 締め切り管理も自動化

### 2.2 ユーザーシナリオ

#### シナリオ1: 新規ファンの投票キャッチアップ
```
👤 ユーザー: 「BTSファンになったばかりで、どの投票に参加すればいいか分からない...」

1. Votesタブを開く
2. 検索バーで「BTS」と入力
3. 「BTS 2024 必須投票リスト」というコレクションを発見
4. 「全て追加」をタップ
5. 15個の投票タスクが自動的にTASKSに追加される
6. HOME画面で緊急度順に表示され、投票を開始

✅ 結果: 数分で必要な投票を全て把握し、投票活動を開始できた
```

#### シナリオ2: 熱心なファンのキュレーション活動
```
👤 ユーザー: 「BTSの投票を全て追跡している。これを他のARMYと共有したい！」

1. +ボタンから投票タスクを12個登録
2. Votesタブ→「マイコレクション」→「新規作成」
3. 登録したタスクから12個選択
4. タイトル: 「BTS MAMA 2024 完全投票パック」
5. 説明: 「BTSのMAMA 2024投票を網羅！期限別にまとめています。」
6. タグ: #BTS #MAMA2024 #授賞式
7. 公開設定: 全体公開
8. 「公開」ボタンをタップ

✅ 結果: コレクションが公開され、他のARMYが発見・保存できるようになった
```

#### シナリオ3: イベント前の集中投票活動
```
👤 ユーザー: 「MAMA 2024まで1週間。全ての投票を完了させたい！」

1. Votesタブで「MAMA2024」タグを検索
2. 3つのコレクションを発見（BTS版、BLACKPINK版、マルチアーティスト版）
3. 各コレクションの「詳細を見る」をタップして内容確認
4. BTS版とBLACKPINK版を「保存」
5. 「全て追加」で合計24タスクをTASKSに追加
6. HOME画面で締め切り順にソートされ、計画的に投票

✅ 結果: 漏れなく全ての投票に参加できた
```

---

## 3. 機能詳細

### 3.1 Phase 1: MVP機能（最小限の実装）

#### 3.1.1 コレクション発見機能

##### 🔍 検索機能
- **フリーワード検索**
  - タイトル・説明・タグで検索
  - 部分一致対応
  - 検索履歴保存（ローカル）
- **フィルター**
  - 期限順（締切が近い順）
  - 人気順（保存数・いいね数）
  - 新着順（作成日時）
- **タグ検索**
  - プリセットタグ（BTS, BLACKPINK, TWICE, MAMA, AAA等）
  - ユーザー生成タグ
  - タグクラウド表示

##### 📊 トレンド表示
- **トレンドコレクション**
  - 過去7日間で最も保存されたコレクション Top 10
  - リアルタイム更新（1時間ごと）
- **注目タグ**
  - 最も使用されているタグ Top 20
  - アーティスト別、イベント別に分類

##### 🎨 カテゴリ別表示
- **アーティスト別**
  - BTS, BLACKPINK, TWICE, Stray Kids等
  - アーティストアイコン表示
- **イベント別**
  - MAMA, AAA, Golden Disc Awards等
  - イベントロゴ表示
- **国別**
  - 韓国投票、日本投票、グローバル投票
  - 国旗アイコン表示

#### 3.1.2 コレクション詳細表示

##### 📋 コレクション情報
- **基本情報**
  - タイトル（50文字以内）
  - 説明（500文字以内）
  - カバー画像
  - 作成者（ユーザー名・アイコン）
  - 作成日時
- **統計情報**
  - 含まれるタスク数
  - 保存数（💾）
  - いいね数（💜）
  - 閲覧数（👁️）
- **タグ一覧**
  - タップでタグ検索に遷移

##### 📝 含まれるタスク一覧
- **タスクカード表示**
  - タイトル
  - 投票URL（リンク）
  - 期限（締切日時）
  - 残り時間（動的計算）
  - External Appバッジ（ある場合）
- **ソート機能**
  - 期限順
  - 登録順
  - タイトル順

##### ⚡ アクション
- **💾 保存する**: コレクションをマイ保存リストに追加
- **📋 全て追加**: 全タスクを自分のTASKSに一括追加
- **🔗 共有**: コレクションURLを共有（将来実装）

#### 3.1.3 マイコレクション作成

##### ✏️ 作成フロー
1. **「新規作成」ボタンをタップ**
2. **基本情報入力**
   - タイトル入力（必須、50文字以内）
   - 説明入力（任意、500文字以内）
   - カバー画像選択（任意）
     - 自分のタスクの画像から選択
     - フォトライブラリから選択
     - デフォルト画像使用
3. **タスク選択**
   - 自分が登録したタスク一覧から選択
   - 複数選択可能（最大50タスク）
   - チェックボックスで選択状態を表示
4. **タグ設定**
   - プリセットタグから選択
   - カスタムタグ入力（最大10個）
5. **公開設定**
   - 全体公開
   - フォロワーのみ
   - 非公開（自分だけ）
6. **「公開」ボタンをタップ**

##### ✅ バリデーション
- タイトル: 必須、1-50文字
- 説明: 任意、最大500文字
- タスク: 最低1個、最大50個
- タグ: 最大10個、各20文字以内
- カバー画像: 最大10MB

#### 3.1.4 保存機能

##### 💾 保存アクション
- **コレクション保存**
  - ワンタップで保存
  - 保存済みアイコン変化（💾 → ✅）
  - 保存解除も可能
- **マイ保存リスト**
  - 保存したコレクション一覧
  - 最新保存順で表示
  - 削除・並び替え可能

#### 3.1.5 一括タスク追加

##### 📋 追加フロー
1. **「全て追加」ボタンをタップ**
2. **確認ダイアログ表示**
   - 「このコレクションの12個のタスクをTASKSに追加しますか？」
   - 「重複タスクはスキップされます」
3. **「追加」をタップ**
4. **バックグラウンド処理**
   - 各タスクをFirestoreに登録
   - 重複チェック（URL重複を検出）
   - 進捗表示（X/12個追加完了）
5. **完了通知**
   - 「12個のタスクを追加しました」
   - 「TASKSタブで確認できます」

##### 🔍 重複チェック
- **判定基準**: URL完全一致
- **処理**:
  - 重複タスクはスキップ
  - 新規タスクのみ追加
  - 完了後にサマリー表示（例: 「12個中10個を追加（2個は重複のためスキップ）」）

---

### 3.2 Phase 2: エンゲージメント機能

#### 3.2.1 いいね機能
- コレクションにいいね
- 自分がいいねしたコレクション一覧
- いいね数のリアルタイム更新

#### 3.2.2 コメント機能
- コレクションへのコメント投稿
- コメント一覧表示
- コメント通知

#### 3.2.3 フォロー機能
- ユーザーフォロー
- フォロー中のユーザーの新規コレクション通知
- フォロワー・フォロー中一覧

#### 3.2.4 通知システム
- 新規コレクション通知（フォロー中）
- いいね通知
- コメント通知
- 保存数マイルストーン通知（100保存達成など）

---

### 3.3 Phase 3: 高度な機能

#### 3.3.1 AI推奨
- ユーザーの推し設定に基づくコレクション推奨
- 投票履歴からのパーソナライズ
- 類似コレクション提案

#### 3.3.2 達成バッジシステム
- 「キュレーター」バッジ（10コレクション作成）
- 「人気キュレーター」バッジ（1000保存達成）
- 「貢献者」バッジ（100タスク共有）

#### 3.3.3 ランキング・統計
- 週間人気コレクションランキング
- 人気キュレーターランキング
- カテゴリ別トレンド分析

---

## 4. データモデル

### 4.1 VoteCollection

#### TypeScript型定義
```typescript
interface VoteCollection {
  // 基本情報
  collectionId: string;            // Firestore自動生成ID
  creatorId: string;               // 作成者のUID
  creatorName: string;             // 作成者の表示名
  creatorAvatarUrl?: string;       // 作成者のアバターURL

  // コレクション内容
  title: string;                   // タイトル（50文字以内）
  description: string;             // 説明（500文字以内）
  coverImage?: string;             // カバー画像URL
  tags: string[];                  // タグ配列（最大10個）

  // 含まれるタスク
  tasks: VoteTaskInCollection[];   // タスク配列（最大50個）
  taskCount: number;               // タスク数（冗長だが検索高速化のため）

  // 公開設定
  visibility: 'public' | 'followers' | 'private';

  // エンゲージメント統計
  likeCount: number;               // いいね数
  saveCount: number;               // 保存数
  viewCount: number;               // 閲覧数
  commentCount: number;            // コメント数

  // タイムスタンプ
  createdAt: Timestamp;
  updatedAt: Timestamp;
}

interface VoteTaskInCollection {
  taskId: string;                  // 元タスクのID
  title: string;                   // タスクタイトル
  url: string;                     // 投票URL
  deadline: Timestamp;             // 締切
  externalAppId?: string;          // 外部アプリID
  externalAppName?: string;        // 外部アプリ名
  externalAppIconUrl?: string;     // 外部アプリアイコン
  coverImage?: string;             // カバー画像
  orderIndex: number;              // 並び順（0から始まる）
}
```

#### Swift構造体
```swift
struct VoteCollection: Codable, Identifiable {
    let id: String                  // collectionId
    let creatorId: String
    let creatorName: String
    let creatorAvatarUrl: String?

    var title: String
    var description: String
    var coverImage: String?
    var tags: [String]

    var tasks: [VoteTaskInCollection]
    var taskCount: Int

    var visibility: CollectionVisibility

    var likeCount: Int
    var saveCount: Int
    var viewCount: Int
    var commentCount: Int

    var createdAt: Date
    var updatedAt: Date
}

struct VoteTaskInCollection: Codable, Identifiable {
    let id: String                  // taskId
    let title: String
    let url: String
    let deadline: Date
    let externalAppId: String?
    let externalAppName: String?
    let externalAppIconUrl: String?
    let coverImage: String?
    let orderIndex: Int
}

enum CollectionVisibility: String, Codable {
    case publicVisibility = "public"
    case followers = "followers"
    case privateVisibility = "private"
}
```

---

### 4.2 UserCollectionSave

#### TypeScript型定義
```typescript
interface UserCollectionSave {
  userId: string;                  // ユーザーUID
  collectionId: string;            // コレクションID
  savedAt: Timestamp;              // 保存日時
  addedToTasks: boolean;           // TASKSに追加済みか
  addedTaskIds?: string[];         // 追加したタスクID配列
}
```

#### Swift構造体
```swift
struct UserCollectionSave: Codable, Identifiable {
    var id: String { "\(userId)_\(collectionId)" }
    let userId: String
    let collectionId: String
    let savedAt: Date
    var addedToTasks: Bool
    var addedTaskIds: [String]?
}
```

---

### 4.3 UserFollow (Phase 2)

#### TypeScript型定義
```typescript
interface UserFollow {
  followerId: string;              // フォローする人のUID
  followingId: string;             // フォローされる人のUID
  followedAt: Timestamp;           // フォロー日時
}
```

#### Swift構造体
```swift
struct UserFollow: Codable, Identifiable {
    var id: String { "\(followerId)_\(followingId)" }
    let followerId: String
    let followingId: String
    let followedAt: Date
}
```

---

### 4.4 CollectionLike (Phase 2)

#### TypeScript型定義
```typescript
interface CollectionLike {
  userId: string;                  // いいねしたユーザーUID
  collectionId: string;            // コレクションID
  likedAt: Timestamp;              // いいね日時
}
```

---

### 4.5 CollectionComment (Phase 2)

#### TypeScript型定義
```typescript
interface CollectionComment {
  commentId: string;               // Firestore自動生成ID
  collectionId: string;            // コレクションID
  userId: string;                  // コメントしたユーザーUID
  userName: string;                // ユーザー名
  userAvatarUrl?: string;          // ユーザーアバター
  content: string;                 // コメント内容（500文字以内）
  createdAt: Timestamp;            // 投稿日時
  updatedAt: Timestamp;            // 更新日時
}
```

---

## 5. API仕様

### 5.1 コレクション管理API

#### 5.1.1 コレクション一覧取得
```typescript
GET /api/collections

Query Parameters:
  - page: number (default: 1)
  - limit: number (default: 20, max: 50)
  - sortBy: 'latest' | 'popular' | 'trending' (default: 'trending')
  - tags: string[] (フィルター用タグ配列)
  - visibility: 'public' | 'followers' | 'private' (default: 'public')

Headers:
  - Authorization: Bearer <Firebase ID Token>

Response: 200 OK
{
  "success": true,
  "data": {
    "collections": [
      {
        "collectionId": "coll_123",
        "creatorId": "user_abc",
        "creatorName": "K-POP Master",
        "creatorAvatarUrl": "https://...",
        "title": "BTS MAMA 2024 完全投票パック",
        "description": "BTSのMAMA 2024投票を網羅！",
        "coverImage": "https://...",
        "tags": ["BTS", "MAMA2024", "授賞式"],
        "taskCount": 12,
        "visibility": "public",
        "likeCount": 245,
        "saveCount": 89,
        "viewCount": 1523,
        "commentCount": 34,
        "createdAt": "2025-01-10T10:00:00Z",
        "updatedAt": "2025-01-12T15:30:00Z"
      },
      // ...more collections
    ],
    "pagination": {
      "currentPage": 1,
      "totalPages": 10,
      "totalCount": 195,
      "hasNext": true
    }
  }
}

Error Responses:
  - 401 Unauthorized: 認証エラー
  - 400 Bad Request: パラメータエラー
```

---

#### 5.1.2 コレクション詳細取得
```typescript
GET /api/collections/:collectionId

Headers:
  - Authorization: Bearer <Firebase ID Token>

Response: 200 OK
{
  "success": true,
  "data": {
    "collection": {
      // VoteCollection全フィールド
      "collectionId": "coll_123",
      "creatorId": "user_abc",
      // ... (全フィールド)
      "tasks": [
        {
          "taskId": "task_001",
          "title": "MAMA Best Male Group",
          "url": "https://vote.mnet.com/...",
          "deadline": "2025-12-31T23:59:59Z",
          "externalAppId": "app_mnet",
          "externalAppName": "Mnet Vote",
          "externalAppIconUrl": "https://...",
          "coverImage": "https://...",
          "orderIndex": 0
        },
        // ... 11 more tasks
      ]
    },
    "isSaved": true,           // 現在のユーザーが保存済みか
    "isLiked": false,          // いいね済みか (Phase 2)
    "isOwner": false           // 作成者本人か
  }
}

Error Responses:
  - 404 Not Found: コレクションが存在しない
  - 403 Forbidden: 非公開コレクションへの不正アクセス
```

---

#### 5.1.3 コレクション検索
```typescript
GET /api/collections/search

Query Parameters:
  - q: string (検索クエリ、必須)
  - page: number (default: 1)
  - limit: number (default: 20, max: 50)
  - sortBy: 'relevance' | 'latest' | 'popular' (default: 'relevance')
  - tags: string[] (フィルター用タグ配列)

Headers:
  - Authorization: Bearer <Firebase ID Token>

Response: 200 OK
{
  "success": true,
  "data": {
    "collections": [...],      // コレクション配列
    "pagination": {...},       // ページネーション情報
    "searchQuery": "BTS"       // 検索クエリ
  }
}
```

---

#### 5.1.4 トレンドコレクション取得
```typescript
GET /api/collections/trending

Query Parameters:
  - limit: number (default: 10, max: 50)
  - period: '24h' | '7d' | '30d' (default: '7d')

Headers:
  - Authorization: Bearer <Firebase ID Token>

Response: 200 OK
{
  "success": true,
  "data": {
    "collections": [...],      // トレンドコレクション配列
    "period": "7d",
    "generatedAt": "2025-01-13T10:00:00Z"
  }
}
```

---

#### 5.1.5 コレクション作成
```typescript
POST /api/collections

Headers:
  - Authorization: Bearer <Firebase ID Token>
  - Content-Type: application/json

Request Body:
{
  "title": "BTS MAMA 2024 完全投票パック",
  "description": "BTSのMAMA 2024投票を網羅！期限別にまとめています。",
  "coverImage": "https://...",
  "tags": ["BTS", "MAMA2024", "授賞式"],
  "tasks": [
    {
      "taskId": "task_001",
      "orderIndex": 0
    },
    // ... more tasks
  ],
  "visibility": "public"
}

Response: 201 Created
{
  "success": true,
  "data": {
    "collectionId": "coll_new_123",
    "createdAt": "2025-01-13T10:00:00Z"
  }
}

Error Responses:
  - 400 Bad Request: バリデーションエラー
  - 401 Unauthorized: 認証エラー
```

---

#### 5.1.6 コレクション更新
```typescript
PUT /api/collections/:collectionId

Headers:
  - Authorization: Bearer <Firebase ID Token>
  - Content-Type: application/json

Request Body:
{
  "title": "BTS MAMA 2024 完全投票パック（更新版）",
  "description": "...",
  "coverImage": "https://...",
  "tags": ["BTS", "MAMA2024"],
  "tasks": [...],
  "visibility": "public"
}

Response: 200 OK
{
  "success": true,
  "data": {
    "collectionId": "coll_123",
    "updatedAt": "2025-01-13T11:00:00Z"
  }
}

Error Responses:
  - 403 Forbidden: 作成者以外が更新しようとした
  - 404 Not Found: コレクションが存在しない
```

---

#### 5.1.7 コレクション削除
```typescript
DELETE /api/collections/:collectionId

Headers:
  - Authorization: Bearer <Firebase ID Token>

Response: 200 OK
{
  "success": true,
  "message": "Collection deleted successfully"
}

Error Responses:
  - 403 Forbidden: 作成者以外が削除しようとした
  - 404 Not Found: コレクションが存在しない
```

---

### 5.2 保存・エンゲージメントAPI

#### 5.2.1 コレクション保存
```typescript
POST /api/collections/:collectionId/save

Headers:
  - Authorization: Bearer <Firebase ID Token>

Response: 200 OK
{
  "success": true,
  "data": {
    "saved": true,
    "savedAt": "2025-01-13T10:00:00Z"
  }
}
```

---

#### 5.2.2 コレクション保存解除
```typescript
DELETE /api/collections/:collectionId/save

Headers:
  - Authorization: Bearer <Firebase ID Token>

Response: 200 OK
{
  "success": true,
  "data": {
    "saved": false
  }
}
```

---

#### 5.2.3 全タスク追加
```typescript
POST /api/collections/:collectionId/add-to-tasks

Headers:
  - Authorization: Bearer <Firebase ID Token>

Response: 200 OK
{
  "success": true,
  "data": {
    "addedCount": 10,
    "skippedCount": 2,
    "totalCount": 12,
    "addedTaskIds": ["task_001", "task_002", ...],
    "message": "10個のタスクを追加しました（2個は重複のためスキップ）"
  }
}

Error Responses:
  - 404 Not Found: コレクションが存在しない
  - 400 Bad Request: 既に全て追加済み
```

---

#### 5.2.4 マイ保存コレクション一覧
```typescript
GET /api/users/me/saved-collections

Query Parameters:
  - page: number (default: 1)
  - limit: number (default: 20)

Headers:
  - Authorization: Bearer <Firebase ID Token>

Response: 200 OK
{
  "success": true,
  "data": {
    "collections": [...],      // 保存したコレクション配列
    "pagination": {...}
  }
}
```

---

#### 5.2.5 マイコレクション一覧
```typescript
GET /api/users/me/collections

Query Parameters:
  - page: number (default: 1)
  - limit: number (default: 20)
  - visibility: 'all' | 'public' | 'private' (default: 'all')

Headers:
  - Authorization: Bearer <Firebase ID Token>

Response: 200 OK
{
  "success": true,
  "data": {
    "collections": [...],      // 自分が作成したコレクション配列
    "pagination": {...}
  }
}
```

---

### 5.3 Phase 2 API（将来実装）

#### 5.3.1 いいね関連
```typescript
POST   /api/collections/:collectionId/like      # いいね
DELETE /api/collections/:collectionId/like      # いいね解除
GET    /api/collections/:collectionId/likes     # いいねユーザー一覧
```

#### 5.3.2 コメント関連
```typescript
POST   /api/collections/:collectionId/comments       # コメント投稿
GET    /api/collections/:collectionId/comments       # コメント一覧
PUT    /api/comments/:commentId                      # コメント編集
DELETE /api/comments/:commentId                      # コメント削除
```

#### 5.3.3 フォロー関連
```typescript
POST   /api/users/:userId/follow        # フォロー
DELETE /api/users/:userId/follow        # フォロー解除
GET    /api/users/:userId/followers     # フォロワー一覧
GET    /api/users/:userId/following     # フォロー中一覧
```

---

## 6. UI設計

### 6.1 Discover画面（メイン）

#### 画面構成
```
┌─────────────────────────────────────┐
│ ← Discover           🔍 🔔          │ ← Navigation Bar
├─────────────────────────────────────┤
│ [🔍 検索バー: "アーティスト、タグ検索"] │
├─────────────────────────────────────┤
│ 🔥 トレンド                          │
│ ┌───────┐ ┌───────┐ ┌───────┐     │
│ │[画像] │ │[画像] │ │[画像] │ ... │ ← Horizontal Scroll
│ │ BTS   │ │MAMA   │ │年末   │     │
│ │投票集 │ │2024   │ │授賞式 │     │
│ │💜 245 │ │💜 189 │ │💜 156 │     │
│ └───────┘ └───────┘ └───────┘     │
├─────────────────────────────────────┤
│ 📊 カテゴリ                          │
│ [アーティスト] [イベント] [国別]      │ ← Segmented Control
├─────────────────────────────────────┤
│ 人気のコレクション                    │ ← Title
│ ┌───────────────────────────────┐  │
│ │ [カバー画像]                   │  │
│ │ BTS MAMA 2024完全投票パック     │  │
│ │ by @kpop_master                │  │
│ │ 💜 245  💾 89  📋 12           │  │
│ │ #BTS #MAMA2024 #授賞式         │  │
│ └───────────────────────────────┘  │
│ ┌───────────────────────────────┐  │
│ │ [カバー画像]                   │  │
│ │ BLACKPINK 最新投票リスト       │  │
│ │ by @blink_queen                │  │
│ │ 💜 189  💾 67  📋 8            │  │
│ │ #BLACKPINK #投票               │  │
│ └───────────────────────────────┘  │
│                                     │
│ [もっと見る]                         │
├─────────────────────────────────────┤
│ 👥 フォロー中の更新 (Phase 2)        │
│ ...                                 │
└─────────────────────────────────────┘
```

#### UIコンポーネント
- **SearchBar**: 検索バーコンポーネント
- **TrendingCollectionCard**: トレンドコレクションカード（小サイズ）
- **CategorySegmentedControl**: カテゴリ選択
- **CollectionCard**: コレクションカード（大サイズ）

---

### 6.2 コレクション詳細画面

#### 画面構成
```
┌─────────────────────────────────────┐
│ ← BTS MAMA 2024完全投票パック        │ ← Navigation Bar
│                              🔗 ⋯   │
├─────────────────────────────────────┤
│ [カバー画像 - 縦横比16:9]            │ ← Hero Image
├─────────────────────────────────────┤
│ 👤 @kpop_master                     │
│ 💜 245  💾 89  👁️ 1.5K             │
│                                     │
│ BTSのMAMA 2024投票を網羅！          │
│ 期限別にまとめています。             │
│                                     │
│ #BTS #MAMA2024 #授賞式              │
├─────────────────────────────────────┤
│ 📋 含まれるタスク (12)               │ ← Task List Header
│ [期限順 ▼] [登録順] [タイトル順]     │ ← Sort Options
├─────────────────────────────────────┤
│ ┌───────────────────────────────┐  │
│ │ MAMA Best Male Group           │  │
│ │ 🔗 vote.mnet.com               │  │
│ │ ⏰ 期限: 12/31 23:59           │  │
│ │ ⏳ 残り: 18日                  │  │
│ └───────────────────────────────┘  │
│ ┌───────────────────────────────┐  │
│ │ MAMA Worldwide Fans' Choice    │  │
│ │ 🔗 vote.mnet.com               │  │
│ │ ⏰ 期限: 12/25 23:59           │  │
│ │ ⏳ 残り: 12日                  │  │
│ └───────────────────────────────┘  │
│ ... (10 more tasks)                │
├─────────────────────────────────────┤
│ [💾 保存する] [📋 全て追加]          │ ← Action Buttons
└─────────────────────────────────────┘
```

#### UIコンポーネント
- **CollectionHeroView**: カバー画像 + 基本情報
- **CollectionMetaView**: 統計情報（いいね・保存・閲覧数）
- **CollectionDescriptionView**: 説明文
- **TagCloudView**: タグ一覧
- **TaskListView**: タスク一覧（ソート機能付き）
- **TaskItemCard**: 個別タスクカード
- **ActionButtonsView**: 保存・追加ボタン

---

### 6.3 マイコレクション作成画面

#### 画面構成
```
┌─────────────────────────────────────┐
│ ✕ 新規コレクション作成      [完了]   │ ← Navigation Bar
├─────────────────────────────────────┤
│ カバー画像                           │
│ [画像選択エリア]                     │ ← Image Picker
│ - タスクの画像から選択               │
│ - フォトライブラリ                   │
│ - デフォルト画像                     │
├─────────────────────────────────────┤
│ タイトル *                          │
│ [_________________________]         │ ← Text Field
│                              0/50   │
├─────────────────────────────────────┤
│ 説明                                │
│ [_________________________]         │ ← Text View
│ [_________________________]         │
│ [_________________________]         │
│                             0/500   │
├─────────────────────────────────────┤
│ タグ                                │
│ [#BTS] [#MAMA2024] [+追加]         │ ← Tag Chips
│                             0/10    │
├─────────────────────────────────────┤
│ 含めるタスク * (0/50)                │
│ ┌─────────────────────────────┐    │
│ │ ☑ MAMA Best Male Group      │    │ ← Checkboxes
│ │ ☐ MAMA Worldwide Fans' Choice│   │
│ │ ☑ AAA Best Artist           │    │
│ │ ... (more tasks)             │    │
│ └─────────────────────────────┘    │
├─────────────────────────────────────┤
│ 公開設定                            │
│ ○ 全体公開                          │ ← Radio Buttons
│ ○ フォロワーのみ (Phase 2)          │
│ ○ 非公開                            │
└─────────────────────────────────────┘
```

#### UIコンポーネント
- **ImagePickerView**: カバー画像選択
- **TitleTextField**: タイトル入力（文字数カウンター付き）
- **DescriptionTextView**: 説明入力（複数行）
- **TagInputView**: タグ入力・追加
- **TaskChecklistView**: タスク選択チェックリスト
- **VisibilitySelector**: 公開設定選択

---

### 6.4 マイ保存リスト画面

#### 画面構成
```
┌─────────────────────────────────────┐
│ ← マイ保存リスト                     │
├─────────────────────────────────────┤
│ [最新保存順 ▼] [削除モード]         │
├─────────────────────────────────────┤
│ ┌───────────────────────────────┐  │
│ │ [カバー画像]                   │  │
│ │ BTS MAMA 2024完全投票パック     │  │
│ │ by @kpop_master                │  │
│ │ 💾 保存済み • 12/30             │  │
│ │ ✅ タスク追加済み              │  │
│ └───────────────────────────────┘  │
│ ┌───────────────────────────────┐  │
│ │ [カバー画像]                   │  │
│ │ BLACKPINK 最新投票リスト       │  │
│ │ by @blink_queen                │  │
│ │ 💾 保存済み • 12/28             │  │
│ │ [📋 全て追加]                  │  │
│ └───────────────────────────────┘  │
│ ...                                 │
└─────────────────────────────────────┘
```

---

### 6.5 マイコレクション一覧画面

#### 画面構成
```
┌─────────────────────────────────────┐
│ ← マイコレクション        [+ 作成]   │
├─────────────────────────────────────┤
│ [全て] [公開] [非公開]              │ ← Filter Tabs
├─────────────────────────────────────┤
│ ┌───────────────────────────────┐  │
│ │ [カバー画像]                   │  │
│ │ BTS MAMA 2024完全投票パック     │  │
│ │ 🌐 公開 • 12個のタスク          │  │
│ │ 💜 245  💾 89  👁️ 1.5K         │  │
│ │ [編集] [削除]                  │  │
│ └───────────────────────────────┘  │
│ ┌───────────────────────────────┐  │
│ │ [カバー画像]                   │  │
│ │ 個人用BTS投票メモ              │  │
│ │ 🔒 非公開 • 5個のタスク         │  │
│ │ [編集] [削除]                  │  │
│ └───────────────────────────────┘  │
│ ...                                 │
└─────────────────────────────────────┘
```

---

## 7. Firestoreスキーマ

### 7.1 コレクション構造

```
firestore/
├── collections/                      # 投票コレクション
│   └── {collectionId}/
│       ├── metadata                  # コレクションメタデータ
│       └── tasks/                    # サブコレクション: 含まれるタスク
│           └── {taskId}/             # タスク情報
│
├── user_collection_saves/            # ユーザーの保存情報
│   └── {userId}/
│       └── saves/
│           └── {collectionId}/       # 保存したコレクション
│
├── user_follows/                     # フォロー関係 (Phase 2)
│   └── {userId}/
│       ├── following/
│       │   └── {followingUserId}/
│       └── followers/
│           └── {followerUserId}/
│
├── collection_likes/                 # いいね情報 (Phase 2)
│   └── {collectionId}/
│       └── likes/
│           └── {userId}/
│
└── collection_comments/              # コメント (Phase 2)
    └── {collectionId}/
        └── comments/
            └── {commentId}/
```

---

### 7.2 Firestoreセキュリティルール

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // ユーザー認証チェック
    function isSignedIn() {
      return request.auth != null;
    }

    // ユーザー本人チェック
    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    // コレクション作成者チェック
    function isCollectionOwner(collectionId) {
      return isSignedIn() &&
             get(/databases/$(database)/documents/collections/$(collectionId)).data.creatorId == request.auth.uid;
    }

    // === Collections ===
    match /collections/{collectionId} {
      // 公開コレクションは全員が読み取り可能
      allow read: if isSignedIn() &&
                     (resource.data.visibility == 'public' ||
                      (resource.data.visibility == 'followers' &&
                       exists(/databases/$(database)/documents/user_follows/$(resource.data.creatorId)/followers/$(request.auth.uid))) ||
                      isOwner(resource.data.creatorId));

      // 作成は認証済みユーザーのみ
      allow create: if isSignedIn() &&
                       request.resource.data.creatorId == request.auth.uid &&
                       request.resource.data.title is string &&
                       request.resource.data.title.size() > 0 &&
                       request.resource.data.title.size() <= 50 &&
                       request.resource.data.tasks.size() >= 1 &&
                       request.resource.data.tasks.size() <= 50 &&
                       request.resource.data.tags.size() <= 10;

      // 更新・削除は作成者のみ
      allow update, delete: if isCollectionOwner(collectionId);

      // サブコレクション: tasks
      match /tasks/{taskId} {
        allow read: if isSignedIn();
        allow write: if isCollectionOwner(collectionId);
      }
    }

    // === User Collection Saves ===
    match /user_collection_saves/{userId}/saves/{collectionId} {
      // 読み取り・書き込みは本人のみ
      allow read, write: if isOwner(userId);
    }

    // === User Follows (Phase 2) ===
    match /user_follows/{userId}/following/{followingUserId} {
      allow read: if isSignedIn();
      allow create, delete: if isOwner(userId);
    }

    match /user_follows/{userId}/followers/{followerUserId} {
      allow read: if isSignedIn();
      allow create, delete: if isOwner(followerUserId);
    }

    // === Collection Likes (Phase 2) ===
    match /collection_likes/{collectionId}/likes/{userId} {
      allow read: if isSignedIn();
      allow write: if isOwner(userId);
    }

    // === Collection Comments (Phase 2) ===
    match /collection_comments/{collectionId}/comments/{commentId} {
      allow read: if isSignedIn();
      allow create: if isSignedIn() &&
                       request.resource.data.userId == request.auth.uid &&
                       request.resource.data.content is string &&
                       request.resource.data.content.size() > 0 &&
                       request.resource.data.content.size() <= 500;
      allow update, delete: if isSignedIn() &&
                               resource.data.userId == request.auth.uid;
    }
  }
}
```

---

### 7.3 Firestoreインデックス

```json
{
  "indexes": [
    {
      "collectionGroup": "collections",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "visibility", "order": "ASCENDING" },
        { "fieldPath": "saveCount", "order": "DESCENDING" },
        { "fieldPath": "createdAt", "order": "DESCENDING" }
      ]
    },
    {
      "collectionGroup": "collections",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "tags", "arrayConfig": "CONTAINS" },
        { "fieldPath": "saveCount", "order": "DESCENDING" }
      ]
    },
    {
      "collectionGroup": "collections",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "visibility", "order": "ASCENDING" },
        { "fieldPath": "createdAt", "order": "DESCENDING" }
      ]
    },
    {
      "collectionGroup": "collections",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "creatorId", "order": "ASCENDING" },
        { "fieldPath": "createdAt", "order": "DESCENDING" }
      ]
    }
  ],
  "fieldOverrides": []
}
```

---

## 8. 既存機能との連携

### 8.1 HOME画面との連携

#### CommunityActivityView統合
**現在**: `CommunityActivityView` は静的コンテンツ表示
**拡張**: Votesタブの最新アクティビティを表示

```swift
// ContentView.swift (HomeView) の CommunityActivityView
CommunityActivityView()
    .padding(.horizontal)

// ↓ 拡張後

CommunityActivityCard(
    userName: "K-Pop Fan",
    action: "新しいコレクションを作成しました",
    collectionTitle: "BTS MAMA 2024完全投票パック",
    timeAgo: "2 hours ago"
)
.onTapGesture {
    // Votesタブに遷移してコレクション詳細を表示
    selectedTab = 1  // Votesタブ
    openCollection(collectionId: "coll_123")
}
```

#### View Allボタン
- CommunityActivityViewの「View All」ボタン
- タップでVotesタブに遷移

---

### 8.2 TASKSタブとの連携

#### 一括タスク追加
- Votesタブで「全て追加」をタップ
- バックグラウンドで各タスクをFirestoreに登録
- TASKSタブの「Active」に自動追加

#### マイコレクション作成
- TASKSタブで表示されている自分のタスクから選択
- Votesタブの「マイコレクション作成」に自動連携

---

### 8.3 +ボタン（タスク登録）との連携

#### 新規タスク登録時の連携
```swift
// TaskRegistrationView.swift に追加

// タスク登録後
Button("コレクションに追加") {
    showCollectionPicker = true
}
.sheet(isPresented: $showCollectionPicker) {
    CollectionPickerView(taskId: registeredTaskId)
}
```

#### 既存コレクションへの追加
- タスク登録完了後、「既存コレクションに追加」オプション
- マイコレクション一覧から選択
- 選択したコレクションに追加

---

### 8.4 Profileタブとの連携

#### プロフィール画面に追加
```swift
// ProfileView.swift に追加

VStack(spacing: Constants.Spacing.small) {
    Text("Community")
        .font(.system(size: Constants.Typography.titleSize, weight: .bold))
        .foregroundColor(Constants.Colors.textWhite)
        .padding(.horizontal)

    VStack(spacing: 0) {
        SettingsRow(
            icon: "square.grid.2x2.fill",
            title: "マイコレクション",
            color: Constants.Colors.accentPink
        )
        .onTapGesture {
            // Votesタブのマイコレクション画面に遷移
        }

        Divider().padding(.leading, 60).background(Constants.Colors.textGray.opacity(0.3))

        SettingsRow(
            icon: "bookmark.fill",
            title: "保存リスト",
            color: Constants.Colors.accentBlue
        )
        .onTapGesture {
            // Votesタブの保存リスト画面に遷移
        }
    }
    .background(Constants.Colors.cardDark)
    .cornerRadius(16)
    .shadow(color: Color.black.opacity(0.3), radius: 4, x: 0, y: 2)
}
```

---

## 9. 実装ロードマップ

### 9.1 Phase 1: MVP（3-4週間）

#### Week 1: データモデル・API実装
- **Day 1-2**: データモデル作成
  - VoteCollection Swift構造体
  - UserCollectionSave構造体
  - TypeScript型定義
- **Day 3-5**: Cloud Functions API実装
  - `POST /api/collections` - コレクション作成
  - `GET /api/collections` - 一覧取得
  - `GET /api/collections/:id` - 詳細取得
  - `GET /api/collections/search` - 検索
  - `POST /api/collections/:id/save` - 保存
  - `POST /api/collections/:id/add-to-tasks` - 一括追加
- **Day 6-7**: Firestoreスキーマ・ルール設定
  - コレクション構造設定
  - セキュリティルール実装
  - インデックス作成

#### Week 2: iOS UI基礎実装
- **Day 8-10**: Discover画面
  - レイアウト実装
  - 検索バー
  - トレンドセクション
  - コレクションリスト
- **Day 11-12**: コレクション詳細画面
  - Hero画像表示
  - メタ情報表示
  - タスク一覧
  - アクションボタン
- **Day 13-14**: ViewModel実装
  - DiscoverViewModel
  - CollectionDetailViewModel
  - API統合

#### Week 3: 作成・保存機能
- **Day 15-17**: マイコレクション作成画面
  - フォームUI実装
  - 画像選択機能
  - タスク選択チェックリスト
  - バリデーション
- **Day 18-19**: 保存機能実装
  - 保存アクション
  - 保存リスト画面
  - 保存状態管理
- **Day 20-21**: 一括タスク追加機能
  - 追加処理実装
  - 重複チェック
  - 進捗表示

#### Week 4: 統合・テスト
- **Day 22-24**: 既存機能との連携
  - HOME画面連携
  - TASKSタブ連携
  - +ボタン連携
- **Day 25-26**: UI/UX調整
  - デザイン微調整
  - アニメーション追加
  - エラーハンドリング
- **Day 27-28**: テスト・バグ修正
  - 機能テスト
  - UI テスト
  - バグ修正

---

### 9.2 Phase 2: エンゲージメント（2-3週間）

#### Week 5-6: いいね・コメント機能
- いいねAPI実装
- コメントAPI実装
- UI実装

#### Week 7: フォロー・通知機能
- フォローAPI実装
- 通知システム構築
- UI実装

---

### 9.3 Phase 3: 高度な機能（2-3週間）

#### Week 8-9: AI推奨システム
- 推奨アルゴリズム実装
- パーソナライズエンジン

#### Week 10: バッジ・ランキング
- 達成バッジシステム
- ランキング機能

---

## 10. 技術スタック

### 10.1 iOS（フロントエンド）
- **言語**: Swift 5.9+
- **フレームワーク**: SwiftUI
- **アーキテクチャ**: MVVM
- **非同期処理**: async/await
- **状態管理**: @Published, @StateObject, @EnvironmentObject
- **画像処理**: AsyncImage, PhotosUI

### 10.2 バックエンド
- **プラットフォーム**: Firebase
- **Cloud Functions**: Node.js 18, TypeScript
- **データベース**: Cloud Firestore (NoSQL)
- **認証**: Firebase Authentication
- **ストレージ**: Firebase Storage

### 10.3 開発ツール
- **IDE**: Xcode 15+
- **バージョン管理**: Git, GitHub
- **API テスト**: Postman, Insomnia
- **CI/CD**: GitHub Actions (将来実装)

---

## 11. 付録

### 11.1 用語集

| 用語 | 説明 |
|------|------|
| **コレクション** | 投票タスクをまとめたキュレーション |
| **キュレーター** | コレクションを作成・共有するユーザー |
| **保存** | コレクションをマイ保存リストに追加 |
| **一括追加** | コレクション内の全タスクをTASKSに追加 |
| **トレンド** | 人気・話題のコレクション |
| **フォロー** | 他ユーザーの活動を追跡 |
| **いいね** | コレクションへの好意的な評価 |

### 11.2 参考リンク
- [Firebase Firestore ドキュメント](https://firebase.google.com/docs/firestore)
- [SwiftUI チュートリアル](https://developer.apple.com/tutorials/swiftui)
- [Firebase Cloud Functions ガイド](https://firebase.google.com/docs/functions)

---

**最終更新**: 2025年1月13日
**次回レビュー**: Phase 1実装開始前（2025年2月予定）
